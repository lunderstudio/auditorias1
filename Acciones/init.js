$(document).ready(function () {
    cargar_dropdawns();
    cargar_vista_mapa();
    cargar_vista_tabla();

    // fill_card_evaluacion("1", "1", "Incipiente", "1");
    // fill_card_resumen("1", "1", "Incipiente", "1");
});